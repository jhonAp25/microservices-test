server:
  port: 8003
eureka:
  client:
    fetch-registry: false
    register-with-eureka: true
    service-url:
      default-zone: hhtp://localhost:9761/eureka
    instance:
      hostname: localhost